# Origin du site (metadataBase, canonical, sitemap, robots).
# Optionnelle : par défaut https://www.geofmigliacci.dev/ (src/lib/site.ts).
# Utile pour pointer ailleurs (préproduction, préviews). Inlinée au build
# (`next build`) : à définir au moment du build, pas seulement au runtime.
NEXT_PUBLIC_SITE_URL=https://www.geofmigliacci.dev/
